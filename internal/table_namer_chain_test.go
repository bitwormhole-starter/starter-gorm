package internal

// func TestTableNamerChain(t *testing.T) {

// 	chain := &tableNamerChain{}
// 	plist := []int{11, 111, 1111, 11111}

// 	for _, p := range plist {
// 		chain.chain = append(chain.chain, &libgorm.TableNamerRegistration{
// 			Priority: p,
// 		})
// 	}

// 	chain.sort()

// 	for i, item := range chain.chain {
// 		t.Logf("namer[%d].Priority = %d", i, item.Priority)
// 	}
// }
